extends base


block content
	h1= posts.tags[0]
	.meta= "此标签下共有%s篇文章" %(d.get_data(tags=posts.tags[0], return_count=true, with_page=false))
	ul: for post in posts: li
		time(datetime= post.date.format("%Y-%m-%d"))= post.date.format("%b %d %Y")
		a(href=post.url)= post.title